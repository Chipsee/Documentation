.. attention::

    #) RS485 can control the input and output direction automatically. There’s no need to control it from within the software.
    #) The 120Ω match resistor for the **RS485** is **already mounted** by default.
    #) The 120Ω match resistor for the **CAN** bus is **NOT mounted** by default.

:pagebreak:`True`